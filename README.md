# Playwright test automation for react-admin demo

This project contains an E2E test automation framework for the **react-admin demo** application (https://github.com/marmelab/react-admin - Posters Galore **e-commerce admin panel**). 

The test suites are placed inside the application repository (`/playwright` directory) to simulate a real-world development environment.

## Tech Stack
* TypeScript
* Playwright